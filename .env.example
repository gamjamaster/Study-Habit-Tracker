# =============================================================================
# Study Habit Tracker - Environment Variables Template
# =============================================================================
# 이 파일을 .env로 복사하고 실제 값들로 수정하세요
# cp .env.example .env

# Supabase Configuration
# Supabase 프로젝트 대시보드에서 확인할 수 있습니다
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_JWT_SECRET=your-jwt-secret-key

# Frontend Environment Variables
# 프론트엔드에서 사용하는 공개 환경변수들
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# API URL for frontend to connect to backend
# Docker Compose 환경에서는 localhost:8000을 사용
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# 환경변수 설정 방법:
# 1. Supabase 프로젝트 생성 후 Settings > API에서 값들 복사
# 2. JWT Secret은 Settings > API > JWT Settings에서 확인
# 3. 이 파일을 .env로 복사하고 실제 값들로 교체
# =============================================================================